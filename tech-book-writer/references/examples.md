# 使用示例

本文档展示技术书籍写作系统的完整使用流程。

## 示例1: 创作机器学习入门书籍

### 步骤1: 启动项目

```
用户: "我想写一本机器学习入门书籍，目标是让零基础读者30天掌握核心技能"
```

### 步骤2: 需求收集

**AI（主编角色）** 会询问：
- 书籍主题：机器学习入门 ✓
- 目标读者：零基础入门者 ✓
- 预期章节数：建议16章
- 是否需要插图：是（时序图、流程图、数据对比图）
- 是否需要翻译：待定

### 步骤3: 生成大纲

**AI（主编角色）** 生成：

```markdown
# 《机器学习与深度学习实战入门》书籍大纲

## 第一部分: 快速入门（第1-3章）
- 第1章: 机器学习是什么
- 第2章: 开发环境搭建
- 第3章: 数据预处理实战

## 第二部分: 核心算法（第4-7章）
- 第4章: 监督学习-回归算法
- 第5章: 监督学习-分类算法
...

⚠️ 请审核大纲，审核通过后回复"开始写作"
```

### 步骤4: 审核大纲

```
用户: "大纲很好，开始写作"
```

### 步骤5: 内容创作

**AI（主编+美工并行）** 开始工作：

**主编** 编写第1章：
```markdown
# 第1章 机器学习到底是什么

## 本章导读
- 15分钟理解机器学习的本质
- 知道机器学习能做什么、不能做什么
- 建立正确的学习路径

## 核心概念
### 1.1 一句话理解机器学习
机器学习 = 让计算机从数据中自动学习规律

### 1.2 与传统编程的区别
[对比表格]

## 原理解析
[流程图：机器学习工作流程]

## 实战案例
### 1.4 5行代码实现房价预测
```python
# 完整可运行代码
...
```

**美工** 同时生成插图：
1. 流程图（Mermaid）
2. 对比表格（Markdown）
3. 应用场景思维导图

### 步骤6: 质量校对

所有章节完成后，**AI（校对角色）** 运行检查：

```bash
python scripts/proofreading.py \
  --input chapters/ \
  --output 校对报告.md \
  --checks all
```

**输出报告**：
```markdown
# 校对报告

## 整体统计
- 总章节数: 16
- ✅ 检查通过: 15
- ⚠️  需要修改: 1

## 问题详情
### 第3章
❌ 代码示例缺少import pandas

## 修改建议
1. 补充第3章的import语句
```

### 步骤7: 修复问题

**AI（主编角色）** 根据报告修复所有问题。

### 步骤8: 完成

```
✅ 书籍创作完成！

生成的文件：
- 机器学习入门_书籍大纲.md
- chapters/chapter01.md ~ chapter16.md
- images/chapter01/ ~ chapter16/
- code/chapter01/ ~ chapter16/
- reports/校对报告.md
```

---

## 示例2: 为现有章节生成插图

### 场景
已有章节内容，需要补充插图。

```
用户: "帮我为第3章生成对比图表，比较几种数据预处理方法"
```

### AI（美工角色）工作流程

1. **读取第3章内容**
   ```python
   # 提取需要对比的数据
   methods = ['删除缺失', '均值填充', '众数填充', 'KNN填充']
   ```

2. **准备数据**
   ```json
   {
     "xAxis": ["删除缺失", "均值填充", "众数填充", "KNN填充"],
     "series": [
       {
         "name": "准确率",
         "data": [0.75, 0.82, 0.80, 0.88]
       },
       {
         "name": "速度(ms)",
         "data": [10, 50, 45, 120]
       }
     ]
   }
   ```

3. **生成图表**
   ```bash
   python scripts/generate_echart.py \
     --type bar \
     --data chapter03_comparison.json \
     --title "数据预处理方法对比" \
     --output images/chapter03/03_comparison.html
   ```

4. **转换为图片**（如果需要）
   ```bash
   python scripts/html_to_image.py \
     --input images/chapter03/03_comparison.html \
     --output images/chapter03/03_comparison.jpg
   ```

5. **插入章节**
   ```markdown
   ## 3.2 缺失值处理对比
   
   ![数据预处理方法对比](../images/chapter03/03_comparison.jpg)
   
   从图中可以看出...
   ```

---

## 示例3: 使用AI生成概念插图

### 场景
需要一张"神经网络结构"的示意图。

### 前提条件
用户提供火山引擎的 AK/SK。

```
用户: "我的火山引擎AK是xxx，SK是xxx，帮我生成一张神经网络结构的插图"
```

### AI（美工角色）工作

```bash
python scripts/generate_ai_image.py \
  --prompt "神经网络结构图，输入层、隐藏层、输出层，节点和连接，发光效果，深色背景，科技风格" \
  --ak "用户的AK" \
  --sk "用户的SK" \
  --output images/chapter08/08_neural_network.jpg \
  --style realistic \
  --width 1200 \
  --height 800
```

**输出**：
```
🎨 正在生成图片...
   提示词: 神经网络结构图，输入层、隐藏层...
   风格: realistic
   尺寸: 1200x800
✅ 图片生成成功
⬇️  正在下载图片...
✅ 图片已保存: images/chapter08/08_neural_network.jpg

🎉 完成！
```

---

## 示例4: 全书翻译为英文

### 场景
书籍完成后，需要翻译为英文版本。

```
用户: "帮我把这本书翻译成英文"
```

### AI（翻译角色）工作流程

1. **确认翻译需求**
   - 目标语言：英语 ✓
   - 翻译范围：全书
   - 术语处理：使用术语表
   - 代码注释：翻译

2. **准备术语表**（如果没有则创建）
   ```json
   {
     "机器学习": "Machine Learning",
     "深度学习": "Deep Learning",
     "神经网络": "Neural Network",
     "监督学习": "Supervised Learning"
   }
   ```

3. **执行翻译**
   ```bash
   python scripts/translate_book.py \
     --input chapters/ \
     --output translations/en/ \
     --target-lang en \
     --keep-terms glossary.json \
     --translate-comments
   ```

4. **质量检查**
   ```bash
   python scripts/proofreading.py \
     --input translations/en/ \
     --output translations/en/校对报告.md
   ```

5. **完成**
   ```
   ✅ 英文版本已生成！
   
   位置: translations/en/
   - chapter01.md
   - chapter02.md
   ...
   ```

---

## 示例5: 快速生成数据可视化图表

### 场景
需要展示算法性能对比。

### 数据准备

创建 `algorithm_comparison.json`：
```json
{
  "xAxis": ["线性回归", "决策树", "随机森林", "神经网络"],
  "series": [
    {
      "name": "准确率",
      "data": [0.75, 0.82, 0.88, 0.92]
    },
    {
      "name": "训练时间(s)",
      "data": [0.5, 2.0, 10.0, 60.0]
    }
  ]
}
```

### 生成图表

```bash
python scripts/generate_echart.py \
  --type bar \
  --data algorithm_comparison.json \
  --title "算法性能对比" \
  --output charts/algorithm_comparison.html
```

### 查看结果

在浏览器中打开 `charts/algorithm_comparison.html`，看到漂亮的交互式图表。

如果需要静态图片：
```bash
python scripts/html_to_image.py \
  --input charts/algorithm_comparison.html \
  --output images/algorithm_comparison.jpg
```

---

## 示例6: 自定义写作规范

### 场景
团队有特定的写作风格要求。

### 创建自定义规范

在项目根目录创建 `技术书籍写作规范.md`：

```markdown
# 我们团队的写作规范

## 章节结构
[自定义结构]

## 代码风格
[团队代码规范]

## 插图要求
[特定的插图风格]
```

### 使用自定义规范

当主编角色启动时，会自动检测并加载这个文件：

```
用户: "开始写作"

AI: 
✓ 检测到自定义写作规范
✓ 已加载: 技术书籍写作规范.md
✓ 将按照团队规范执行...
```

---

## 示例7: 批量处理多个章节

### 场景
需要为所有章节生成插图。

### 批量脚本

创建 `batch_generate.sh`：
```bash
#!/bin/bash

# 为每个章节生成对应的图表
for i in {01..16}; do
  chapter="chapter$i"
  
  if [ -f "data/${chapter}_data.json" ]; then
    echo "生成 $chapter 的图表..."
    
    python scripts/generate_echart.py \
      --type bar \
      --data "data/${chapter}_data.json" \
      --title "第${i}章数据图表" \
      --output "images/${chapter}/chart.html"
    
    echo "✓ $chapter 完成"
  fi
done

echo "✅ 所有图表生成完成！"
```

### 运行
```bash
chmod +x batch_generate.sh
./batch_generate.sh
```

---

## 常用命令速查

### 质量检查
```bash
# 完整检查
python scripts/proofreading.py --input chapters/ --output 校对报告.md

# 只检查代码
python scripts/proofreading.py --input chapters/ --checks code

# 只检查结构
python scripts/proofreading.py --input chapters/ --checks structure
```

### 生成图表
```bash
# 柱状图
python scripts/generate_echart.py --type bar --data data.json --title "标题" --output chart.html

# 折线图
python scripts/generate_echart.py --type line --data data.json --title "标题" --output chart.html

# 饼图
python scripts/generate_echart.py --type pie --data data.json --title "标题" --output chart.html
```

### AI插图生成
```bash
python scripts/generate_ai_image.py \
  --prompt "图片描述" \
  --ak "YOUR_AK" \
  --sk "YOUR_SK" \
  --output "image.jpg"
```

---

## 最佳实践

### 1. 分阶段创作
- 先完成大纲，获得用户认可
- 逐章编写，及时生成插图
- 每3-5章进行一次校对

### 2. 插图优先级
- 优先使用 Mermaid 流程图（简单、清晰）
- 其次使用 Echart 数据图表（专业、美观）
- 最后考虑 AI 生成（成本高、不可控）

### 3. 质量保证
- 每章完成后立即运行校对
- 所有代码必须实际运行验证
- 测试题必须有完整答案

### 4. 版本管理
- 使用 Git 管理书籍内容
- 每章完成后提交一次
- 重要修改打上标签

---

## 故障排除

### 问题1: 脚本运行失败

**错误**: `ModuleNotFoundError: No module named 'xxx'`

**解决**:
```bash
pip install xxx
```

### 问题2: 图表不显示

**原因**: HTML文件依赖网络加载Echart库

**解决**:
1. 确保有网络连接
2. 或下载Echart到本地

### 问题3: AI图片生成失败

**可能原因**:
- AK/SK错误
- 账户余额不足
- 提示词不符合规范
- 网络问题

**排查**:
```bash
# 测试AK/SK
python scripts/generate_ai_image.py \
  --prompt "测试图片" \
  --ak "YOUR_AK" \
  --sk "YOUR_SK" \
  --output "test.jpg"
```

---

希望这些示例能帮助你快速上手技术书籍写作系统！🚀
